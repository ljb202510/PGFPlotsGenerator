# PGFPlotsGenerator 核心功能手动测试用例

> 版本：v1.1　日期：2026-09-09　适用：AI 图表生成 → PGFPlots 代码 → XeLaTeX 编译 PDF 全链路
>
> **首轮实测结论（2026-09-09）：10/10 全部通过**，其中用例 02–06 为“修复后复测通过”，详见 §7.1。

## 1. 目的与范围

验证系统**核心链路**是否可用：

```
注册/登录 →（可选）上传数据文件 → 输入提示词 → AI 生成 PGFPlots 代码
        → 编译为 PDF → 在线预览图表是否正常
```

共 **10 个简单用例**：

- **用例 01–08**：带配套数据文件上传（CSV 与 Excel 混合，验证两类格式解析）。
- **用例 09–10**：不传文件，验证“无数据集也可基于公开/自带数据出图”。

## 2. 前置准备

测试前请按 `README.md` 第 4 章启动环境（路径相对 `hello/`）：

```bash
# 1. 初始化数据库（库名 X，会 DROP 重建）
mysql -u root -p000
mysql> source 1.sql;
mysql> source migrations/add_notice_feedback_columns.sql;
mysql> source migrations/create_notice_read_table.sql;
mysql> exit;
cd backend && node migrations/001_conversations.js && cd ..

# 2. 启动后端（终端 A）
cd backend
copy .env.example .env   # 至少填写 JWT_SECRET（Linux/macOS 用 cp）
npm install
npm run dev              # http://localhost:3000

# 3. 启动前端（终端 B，回到 hello/ 根目录）
npm run serve            # http://localhost:8080
```

准备事项：

1. 打开 `http://localhost:8080`，注册一个**普通用户账号**并登录（如已有账号可复用）。
2. 数据文件位于 `docs/testdata/` 目录下（与本文档同级）。共 8 个：5 个 `.csv`、3 个 `.xlsx`。
3. 默认模型为 **Qwen3.5**；如某用例连续失败，可点击页面左下“当前模型”切换为 **Deepseek-V4-Flash** 重试。

## 3. 通用测试流程

每个用例按以下步骤操作（“上传数据”一步仅带文件用例需要）：

1. 登录后进入 **AI图表助手** 页面（首页即图表生成页）。
2. 带文件用例：点击输入框下方 **「点击上传」** 选择 `docs/testdata/` 下对应数据文件；上传成功后会提示“已自动选择文件”。
   - 无文件用例：直接进入第 3 步，不选择任何文件。
3. 在输入框中**原样复制**该用例的提示词（见“提示词”小节）。
4. 回车或点发送按钮，等待 AI 思考结束（输入按钮出现“停止”方块期间属正常生成中）。
5. 等待 AI 回复：应出现「生成的 PGFPlots 图表代码」区域与“历史记录已保存”提示。
6. 点击代码区 **「生成PDF」** 按钮，等待编译（按钮转圈）。
7. 弹出 **PDF 预览** 对话框后，检查图表是否正常显示。
8. 关闭预览，在末尾的“测试结果记录表”中记录结果。

## 4. 通用通过标准

对每个用例，同时满足以下条件即判定**通过**：

1. **无红色错误气泡**，AI 正常返回内容且包含可识别的 PGFPlots/LaTeX 图表代码。
2. 出现“历史记录已保存”提示；点击「生成PDF」出现“PDF生成成功”提示，并弹出可正常打开的 PDF 预览。
3. **PDF 图表渲染正常且符合语义**：
   - 图表标题、X/Y 轴标签（或饼图扇区标签）存在且为中文，无乱码；
   - 图内数据与上传数据（或提示词中数据）一致，量级、单位匹配；
   - 有数据点标记时有数值标注；多系列/多分类时图例不遮挡数据主体；
   - 非空白图、无缺失、无编译错误弹窗（弹窗内不出现 `!` 开头的 LaTeX 报错）。
4. 用例专属“验证要点”全部满足。

> 判定辅助：PDF 可放大查看；若怀疑数据不对，可对照第 6 节中该用例的“数据预览”。

## 5. 用例总览

| 编号 | 验证场景 | 图表类型 | 是否上传数据 | 数据文件 |
|------|----------|----------|:----:|----------|
| 01 | 单系列趋势 | 折线图 | 是 | `testdata/01-line-sales.csv` |
| 02 | 分类对比 | 柱状图 | 是 | `testdata/02-bar-workshop.csv` |
| 03 | 占比结构 | 饼图 | 是 | `testdata/03-pie-share.xlsx` |
| 04 | 变量相关性 | 散点图 | 是 | `testdata/04-scatter-ad-sales.csv` |
| 05 | 多系列对比 | 分组柱状图 | 是 | `testdata/05-groupedbar-products.xlsx` |
| 06 | 双系列趋势 | 折线图 | 是 | `testdata/06-multiline-channels.csv` |
| 07 | 累计结构 | 堆积柱状图 | 是 | `testdata/07-stackedbar-regions.xlsx` |
| 08 | 大数值单位 | 柱状图 | 是 | `testdata/08-budget-city.csv` |
| 09 | 公开数据兜底 | 柱状图 | 否 | 无（AI 公开统计） |
| 10 | 内联数据出图 | 柱状图 | 否 | 无（提示词自带数据） |

## 6. 用例明细

### 用例 01：月度销售额折线图

- **目的**：验证上传 CSV 后能生成并编译单系列折线图，趋势与数据点正确。
- **数据文件**：`docs/testdata/01-line-sales.csv`
- **数据预览**：月份（1–12 月），销售额（万元）＝ 86, 92, 105, 98, 112, 126, 119, 138, 150, 143, 162, 175。
- **提示词**：

```
请根据我上传的数据绘制折线图，展示 1—12 月销售额的变化趋势；折线添加数据点标记并在每个数据点上方标注销售额数值（单位：万元）；添加图表标题、X 轴（月份）与 Y 轴（销售额/万元）标签，并加上图例。
```

- **验证要点**：
  1. 折线含 12 个数据点，数值与上表一致；
  2. 每点上方有数值标注、不互相重叠，整条线走势符合数据（整体上行、3 月回落后再升）；
  3. 中文标题与坐标轴标签正常显示。

### 用例 02：各车间产量柱状图

- **目的**：验证 CSV 上传后柱状图分类轴与数值标注正确。
- **数据文件**：`docs/testdata/02-bar-workshop.csv`
- **数据预览**：一车间 4520、二车间 6100、三车间 3890、四车间 5230、五车间 2980（台）。
- **提示词**：

```
请根据我上传的数据绘制柱状图，对比各车间产量；在每根柱子上方标注产量数值（单位：台）；添加图表标题、X 轴（车间）与 Y 轴（产量/台）标签，并加上图例。
```

- **验证要点**：
  1. 5 根柱子，高度从高到低为二车间 > 四车间 > 一车间 > 三车间 > 五车间；
  2. 柱顶数值标注清晰（无边框遮挡、不与标记重叠）；
  3. Y 轴范围合理，最高的二车间（6100）完整显示在轴内。

### 用例 03：各品牌市场份额饼图

- **目的**：验证 Excel(.xlsx) 上传解析与饼图生成。
- **数据文件**：`docs/testdata/03-pie-share.xlsx`
- **数据预览**：品牌A 32%、品牌B 26%、品牌C 18%、品牌D 14%、品牌E 10%（合计 100%）。
- **提示词**：

```
请根据我上传的数据绘制饼图，展示各品牌市场份额占比；为每个扇区标注品牌名称与百分比数值；添加图表标题与图例。
```

- **验证要点**：
  1. 饼图含 5 个扇区，占比大小与数据一致（A 最大、E 最小）；
  2. 各扇区标签含品牌名与百分比，合计约 100%；
  3. 中文字体与图例渲染正常。

### 用例 04：广告费用与销量散点图

- **目的**：验证两列数值型 CSV 生成散点图及坐标量纲。
- **数据文件**：`docs/testdata/04-scatter-ad-sales.csv`
- **数据预览**：广告费用（万元）与销量（件）共 10 组正相关数据（5,42）…（30,205）。
- **提示词**：

```
请根据我上传的数据绘制散点图，观察广告费用与销量之间的相关性；每个数据点使用清晰的圆形标记，不要添加趋势线；添加图表标题、X 轴（广告费用/万元）与 Y 轴（销量/件）标签。
```

- **验证要点**：
  1. 共 10 个散点，整体呈向右上递增的正相关分布；
  2. X 轴范围约 0–32（万元）、Y 轴范围约 0–210（件），坐标与数据匹配；
  3. 散点无越界、无图例遮挡。

### 用例 05：产品 A/B 季度销量分组柱状图

- **目的**：验证多列 Excel 数据生成多系列分组柱状图与图例。
- **数据文件**：`docs/testdata/05-groupedbar-products.xlsx`
- **数据预览**：一季度 产品A 45 / 产品B 38；二季度 52/55；三季度 60/58；四季度 68/74（万件）。
- **提示词**：

```
请根据我上传的数据绘制分组柱状图，对比产品A与产品B在各季度的销量；两类产品使用不同颜色并在柱顶标注销量数值（单位：万件）；添加图表标题、X 轴（季度）与 Y 轴（销量/万件）标签，并加上图例。
```

- **验证要点**：
  1. 每个季度有并排两根柱子（产品A、产品B），共 4 组；
  2. 图例区分产品A/产品B，颜色与柱子一一对应；
  3. 四季度两组数值 68/74 为全场最高，完整显示。

### 用例 06：线上线下渠道双折线对比

- **目的**：验证多列 CSV 生成多系列折线图与图例区分。
- **数据文件**：`docs/testdata/06-multiline-channels.csv`
- **数据预览**：1–12 月 线下渠道（万元）与线上渠道（万元）两列（线上渠道从 9 月起反超线下）。
- **提示词**：

```
请根据我上传的数据绘制折线图，对比线下渠道与线上渠道 1—12 月销售额变化趋势；两条折线使用不同颜色与标记，并标注数值（单位：万元）；添加图表标题、X 轴（月份）与 Y 轴（销售额/万元）标签，并加上图例。
```

- **验证要点**：
  1. 两条折线、两种颜色，图例区分“线下渠道/线上渠道”；
  2. 线上渠道约 9 月后数值反超线下渠道，交叉点可见；
  3. 12 个月 X 轴刻度与数值标注不重叠。

### 用例 07：三区域季度销量堆积柱状图

- **目的**：验证多列 Excel 生成堆积柱状图及累计高度正确。
- **数据文件**：`docs/testdata/07-stackedbar-regions.xlsx`
- **数据预览**：一季度至四季度，北区/中区/南区（万吨）：Q1 35/42/28，Q2 45/48/36，Q3 52/55/44，Q4 60/63/51。
- **提示词**：

```
请根据我上传的数据绘制堆积柱状图，展示北区、中区、南区三个区域各季度的销量累计情况；三个区域使用不同颜色分段堆积并加上图例；添加图表标题、X 轴（季度）与 Y 轴（销量/万吨）标签。
```

- **验证要点**：
  1. 每季度一根“堆积柱”，自下而上为北区→中区→南区三种颜色，逐季增高；
  2. 图例三种颜色对应正确；
  3. 四季度总高约 174（60+63+51）不超出 Y 轴范围。

### 用例 08：各城市一般公共预算收入柱状图（大数值）

- **目的**：验证“亿元”级别大数值时坐标单位与数据量级一致、不出现错位空白。
- **数据文件**：`docs/testdata/08-budget-city.csv`
- **数据预览**：上海 8313、北京 6182、深圳 3712、苏州 2456、杭州 2017、广州 1979（亿元）。
- **提示词**：

```
请根据我上传的数据绘制柱状图，展示各城市的一般公共预算收入；在每根柱子上方标注收入数值（单位：亿元）；添加图表标题、X 轴（城市）与 Y 轴（一般公共预算收入/亿元）标签，并加上图例。请确保 Y 轴数值与数据保持同一单位量级。
```

- **验证要点**：
  1. 6 根柱子，上海最高（8313 亿元），柱顶标注单位一致；
  2. Y 轴刻度单位（亿元）与柱高对应，无因“元/亿”混用造成的过低或过高问题；
  3. 数值全部为真实千位量级、坐标轴不出现异常巨大或微小刻度。

### 用例 09：近五年死亡人口柱状图（公开数据兜底）

- **目的**：验证**不上传文件**时，AI 能基于公开权威统计数据生成并编译图表。
- **数据文件**：无（本用例不上传）。
- **提示词**：

```
请绘制近五年（2019—2023 年）全国死亡人口的柱状图，并在每根柱子上方标注数值（单位：万人）；添加图表标题、X 轴（年份）与 Y 轴（出生人口/万人）标签，并加上图例。
```

- **验证要点**：
  1. 能正常生成 5 根柱子，不需要上传任何文件；
  2. 数值为“万人”量级（约 900–1200），年份为 2019—2023；AI 给出数值与公开统计略有出入时可接受，量级必须合理；
  3. 编译与 PDF 预览正常，无空气泡、无“拒绝生成”。

### 用例 10：粮食产量柱状图（提示词内联数据）

- **目的**：验证无文件时，提示词中自带数据也能直接出图。
- **数据文件**：无（本用例不上传）。
- **提示词**：

```
请根据下面的数据绘制柱状图：某省近五年粮食产量分别为 2019 年 3200 万吨、2020 年 3280 万吨、2021 年 3360 万吨、2022 年 3450 万吨、2023 年 3520 万吨。请在每根柱子上方标注产量数值（单位：万吨）；添加图表标题、X 轴（年份）与 Y 轴（粮食产量/万吨）标签。
```

- **验证要点**：
  1. 5 根柱子逐年递增（3200 → 3520），数据与提示词完全一致；
  2. Y 轴按“万吨”标度，范围约 3000–3600；
  3. 编译与预览正常。

## 7. 测试结果记录表

| 用例 | 是否上传 | AI 生成 | 代码生成 | PDF 编译 | PDF 显示正常 | 结果（通过/失败/存疑） | 备注 |
|:----:|:----:|:----:|:----:|:----:|:----:|:----:|------|
| 01 折线图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 无异常 |
| 02 柱状图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 曾编译失败，修复后复测通过（见 7.1） |
| 03 饼图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 曾双层文档结构/宏包缺失，修复后复测通过（见 7.1） |
| 04 散点图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 曾数值标注覆盖/缺失，修复后复测通过（见 7.1） |
| 05 分组柱状图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 曾分类轴全角逗号+上传名超长，修复后复测通过（见 7.1） |
| 06 双折线图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 曾双系列标注压叠，修复后复测通过（见 7.1） |
| 07 堆积柱状图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 无异常 |
| 08 大数值柱状图 | 是 | ✓ | ✓ | ✓ | ✓ | 通过 | 无异常 |
| 09 公开数据 | 否 | ✓ | ✓ | ✓ | ✓ | 通过 | 无异常 |
| 10 内联数据 | 否 | ✓ | ✓ | ✓ | ✓ | 通过 | 无异常 |

> 结果建议：通过＝全部验证要点满足；存疑＝能出图但个别要点不满足（在备注说明）；失败＝编译失败/空气泡/预览空白。

### 7.1 首轮实测问题与修复记录（2026-09-09）

> 记录首轮实测（10 个用例）期间发现并已修复的问题；除下列条目外，其余均一次通过。以下修复仅影响“新生成请求/后续编译”，不影响历史数据。

#### 用例 02 · 柱状图编译失败（`PDF生成失败`）
- **现象**：点「生成PDF」提示失败，错误提示仅显示 XeTeX 启动横幅（Toast 只展示前 300 字符，真实 LaTeX 报错被截断）。
- **原因**：`compileToPDF` 错误文案 `slice(0,300)` 截断；同时彼时系统提示未严格约束 AI 输出结构与标注样式，编译链路更易踩坑却难以定位。
- **解决方案**：错误 Toast 改为完整展示；编译失败样本与日志留档 `backend/storage/debug/`；系统提示 PE 重构（禁止文档脚手架、修正数值标注样式键）+ 编译外壳加固（预处理无条件清理 `\documentclass/\usepackage`、预置 `pgf-pie`）。
- **复测**：通过。

#### 用例 03 · 饼图双层文档结构导致无饼图
- **现象**：AI 输出的是完整 LaTeX 文档（`\documentclass{standalone}`、`\usepackage{pgf-pie}`、`\begin{document}…\end{document}`），PDF 只渲染出孤立的“数据来源”注记。
- **原因**：AI 输出了文档脚手架，被后端再次包裹形成双层 `\documentclass`；旧 `preprocessLatexCode` 仅在含 `\begin{document}` 时才清理，且外壳未预置 `pgf-pie`。
- **解决方案**：系统提示【输出内容边界】禁止输出文档脚手架并说明预置宏包；`preprocessLatexCode` 无条件行级清除 `documentclass`/`usepackage`；编译外壳预置 `\usepackage{pgf-pie}`。
- **复测**：通过。

#### 用例 04 · 散点图数值标注覆盖数据点 / 偶发缺失
- **现象**：数值标注带白底黑框、盖住数据点；偶发完全不显示数值标注。
- **原因**：提示词使用无效键 `nodes near coords style=`（正确键是 `every node near coord/.append style=`）；对“有 `mark` 必配数值标注”约束不足。
- **解决方案**：系统提示 R3 改为正确样式键并要求无边框无底色（`fill=none, draw=none, inner sep=1pt, anchor=south`），加入正反例与输出前自检，强制标注成对出现。
- **复测**：通过。

#### 用例 05 · 分组柱状图柱子全挤到中间 + 上传050607时 `Data too long`
- **现象（图）**：x 轴刻度变成一整串“一季度，二季度，三季度，四季度”，8 根柱子全部堆在中间。
- **原因**：`symbolic x coords` 列表使用了**全角中文逗号 `，`**，pgfplots 仅以英文半角逗号 `,` 分隔，于是把整串文字当作**单个分类**。
- **解决方案**：系统提示新增 R7——`symbolic x coords` 列表必须用英文半角逗号分隔并附反例。
- **现象（上传）**：上传该文件报 `Data too long for column 'data_name'`。
- **原因**：`data_name` 列原为 `VARCHAR(20)`，文件名去扩展后 22 字符超长。
- **解决方案**：迁移扩列到 `VARCHAR(50)`（`migrations/alter_data_file_data_name.sql`，存量库已执行，`1.sql` 已同步）；`datasets.js` 上传/改名接口对超长名兜底截断到 50；`DataUpload.vue` 输入框 `maxlength="50"`。
- **复测**：通过。

#### 用例 06 · 双折线数值标注互相压盖
- **现象**：两条折线同时标注，在 Y 值接近/交叉的点位（如 6 月 76/75、7 月 90/84）数字互相压盖并贴住线条/数据点。
- **原因**：两条 `\addplot` 共用 axis 级同一个 `every node near coord` 样式且都 `anchor=south`，标在同一侧。
- **解决方案**：系统提示新增 R8——多系列折线（≥2 系列且 X≥8）按系列错开：y 较大系列 `anchor=south`（标上方）、较小系列 `anchor=north`（标下方），字号 `\tiny`/`\scriptsize`，附双 `\addplot` 示例与反例。
- **复测**：通过。

#### 同期修复的其他问题（界面 / 上传 / 提示）
1. **首屏“快速开始模版”遮挡 AI 助手初始消息并阻断拖拽**：将 `guide-area` 移入 `.chat-messages` 容器，初始消息完整可见且整个聊天区可拖拽上传。
2. **错误提示可关闭、可超时**：全局 ElMessage 默认 `duration: 5000ms` + `showClose: true`（显示可点击关闭按钮）。
3. **编译失败可完整查看并留档**：Toast 不再截断；失败 `.tex/.log` 自动保存到 `backend/storage/debug/hist{id}_{时间戳}.tex(.log)`，控制台打印样本路径。


## 8. 常见问题与重试建议

| 现象 | 建议处理 |
|------|----------|
| AI 返回“内容为空”（502）或空气泡 | 重新发送一次同一提示词；仍失败则点击“当前模型”切换为 Deepseek-V4-Flash 重试 |
| 提示“无法连接 API” | 检查 `backend/.env` 中模型 API Key/URL，确认后端启动正常 |
| 点「生成PDF」提示编译失败 | 先重试同一提示词一次；确认代码区不是文字说明而是完整 LaTeX 代码；若多次失败记录报错内容 |
| PDF 图表为空白或数据没画出来 | 多为 AI 生成的 Y 轴单位与数据量级不一致，重新发送时在提示词里明确单位（如用例 08 的写法） |
| 预览提示“浏览器不支持” | 换 Chrome/Edge 再试（PDF 预览依赖浏览器内置查看器） |
| 上传 Excel/CSV 解析异常 | 确认文件直接来自 `docs/testdata/`，勿改动表头；单个文件不超过 100MB |
| 点「生成PDF」提示失败，想定位真实原因 | 错误 Toast 现完整展示；后端会把失败样本与日志保存到 `backend/storage/debug/hist{id}_{时间戳}.tex(.log)`，用该 .tex 可本地复现并交给维护者 |
| 上传报 `Data too long for column 'data_name'` / 数据集名称过长 | 名称最长 50 字符；旧库需先执行 `migrations/alter_data_file_data_name.sql` 扩列（`1.sql` 已内置），上传接口会自动截断兜底 |

---

**完成标准**：10 个用例中 8 个及以上通过，即认为核心链路（提示词 + 上传数据 → 可编译图表 PDF 正常显示）可用；失败用例的备注能定位到具体环节（生成 / 编译 / 渲染）。

**首轮结论（2026-09-09）**：10/10 通过（用例 02–06 为修复后复测通过），问题详情见 §7.1。

**回归建议**：后续若再调整系统提示或编译外壳，请按 §3 通用流程重跑全部用例；编译失败样本自动留档于 `backend/storage/debug/`，便于把 `.tex` 提供给维护者复现排查。
